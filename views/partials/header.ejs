<header>
    <h1>blog</h1>
    <nav>
        <a href="/">home</a>
        <% if(locals.adminIsLogged) {%> 
            <a href="/admin">admin</a>
        <% } %> 
        <% if(locals.session.isLogged) { %>
            <a href="dahsboard">dashboard</a>
        <% } else { %>
            <a href="/entry">sign in/up</a>            
        <% } %> 
    </nav>
</header>