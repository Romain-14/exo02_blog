<main id="home">
    <h2>welcome to my blog <%= locals.session.user?.alias %> </h2>

    <section>
        <% for(const post of posts) { %> 

            <article>
                <a href="/story/<%= post.postID %>">
                    <h3><%= post.Title %></h3>
                    <p><%= post.Contents %></p>
                    <p>By <%= post.FirstName %> <%= post.LastName %></p>
                    <p><%= post.CreationTimestamp.toLocaleDateString() %></p>
                </a>
            </article>

        <% } %> 
    </section>
    
</main>